/* 
 * Author: Brian Klein
 * Date: 1/30/17
 * Program: Monterey Beach.java
 * Purpose: Calculate the rental charge, given the type of moped,
 *              when it is rented, and the hours rented
 */
 
import java.util.Scanner;  //use a Scanner object to represent the keyboard
 
public class Monterey Beach   
{   		
   public static void main(String[] args) 
   {
		
         //create the Scanner object representing the keyboard
      Scanner console = new Scanner(System.in);
                  
         //declare variables
      double fee, extraHours;
      
      if(type.compareTo(50cc))
      {
         if(day is weekday)
         {
            fee = 15
            if(extraHours > 0)
            {
               fee = fee + extraHours * 2.5;
            }
         else //weekend
         {
            fee = 30
            if(extraHours > 0)
            {
               fee = fee + extraHours * 7.5;
            }
      }
      else //250cc
      
   }//end main
   
}//end class